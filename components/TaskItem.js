import React from "react";
import { View, Text } from "react-native";

const TaskItem = () => {
    return (
        <View>
            <Text>desde task item</Text>
        </View>
    );
};

export default TaskItem;
